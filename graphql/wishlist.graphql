type Wishlist {
  id: ID!
  user: User!
  items: [WishlistItem!]!
  createdAt: String!
  updatedAt: String!
}

type WishlistItem {
  product: Product!
  addedAt: String!
}

input WishlistItemInput {
  productId: ID!
}
